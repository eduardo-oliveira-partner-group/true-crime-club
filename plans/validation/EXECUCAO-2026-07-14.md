# Execução do checklist Front — 2026-07-14

**Ambiente:** `http://127.0.0.1:3000` (dev já em execução)  
**API configurada:** `NEXT_PUBLIC_API_BASE_URL=https://tcc-front-office-api.vercel.app/api/`  
**LOCAL_MOCK:** não ativo  
**Typecheck:** `pnpm typecheck` → exit 0  
**API pública:** `GET /planos` → 200, 2 planos  
**Método:** smoke HTTP + inspeção de código/UI (sem sessão autenticada interativa no browser)

Evidência bruta: `_smoke-content.json`

## Resumo

| ID | Cenário | Resultado | Notas |
|----|---------|----------|-------|
| FO-002 | Login | **PASS parcial** | Página completa; área `/cliente/*` acessível como Visitante (sem guard) |
| FO-010 | Logout | **PASS parcial** | Botão Sair chama `auth.logout` + limpa `localStorage`; sem redirect forçado em `/cliente/*` |
| FO-003 | Recuperação de senha | **PASS parcial** | Solicitação + feedback; reset com token **ausente** (copy admite simulação) |
| FO-006 | Área do cliente | **FAIL parcial** | Página carrega; defaults hardcoded; `PATCH` via `cliente-001` |
| FO-007 | Assinaturas | **PASS smoke** | Rota 200, nav "Minhas assinaturas", shell com Sair |
| FO-008 | Pedidos | **PASS smoke** | Rota 200, nav "Meus pedidos" |
| FO-009 | Cartões | **PASS smoke** | Rota 200, nav "Meus cartões"; CRUD no client component |
| FO-004 | Pagamento cartão | **INCONCLUSIVO** | Stepper suporte `credit_card` no código; SSR sem métodos (sem perfil) |
| FO-005 | Pagamento Pix | **INCONCLUSIVO** | Stepper suporte `pix` no código; SSR sem labels Pix/Cartão sem sessão |
| FO-001 | Contratação E2E | **NÃO EXECUTADO** | Depende de login + checkout autenticado |

## Achados (prioridade)

1. **Sem guard de autenticação** em `/cliente/*` — layout renderiza shell com "Visitante"; não redireciona para `/login`.
2. **`cliente-001` hardcoded** em `api-client.ts` (`updateProfile`, endereços) — risco 403 com JWT real.
3. **Perfil** inicializa com dados mock fixos antes do `getProfile` (Mariana Silva…).
4. **Recuperação de senha** só solicita e-mail; sem tela de redefinição.
5. **Checkout** com métodos Pix/Cartão depende de `profile.paymentMethods`; sem auth a lista pode vir vazia e esconder as opções.

## Ordem sugerida pós-checklist

1. Guard de rota `/cliente/*` → `/login` quando `auth.me()` falhar.
2. Substituir `cliente-001` pelo `id` de `auth.me()`.
3. Reexecutar FO-002/004/005/001 com sessão autenticada (manual ou Playwright).
