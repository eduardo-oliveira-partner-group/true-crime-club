# Execução do checklist Front — 2026-07-14 (auth + guard)

**Ambiente de execução autenticada:**
- Front: `http://127.0.0.1:3000` (`NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8001/api`)
- API: `http://127.0.0.1:8001` (`AUTH_MODE=mock`, `SUPABASE_RPC_ENABLED=false`)
- Typecheck: `pnpm typecheck` → exit 0

**Mudanças entregues nesta sessão:**
- Persistência de JWT (`tcc_access_token`) + `Authorization: Bearer` no `api-client`
- Botões **Usuário 1/2** no login preenchem e autenticam
- Guard `RequireAuth` em `/cliente/*` (redirect `/login?next=…` + timeout 8s)
- CORS default da API inclui `http://127.0.0.1:3000`
- `.env.local` aponta para API local (remoto Vercel bloqueia CORS no browser)

## Resultado Playwright (usuários mock)

| Passo | Resultado |
|-------|----------|
| Guard sem sessão → `/cliente/pedidos` | Redirect ` /login?next=%2Fcliente%2Fpedidos` |
| Login Usuário 1 (`carlos.souza@email.com`) | Token salvo; vai para `/cliente/pedidos`; shell mostra **Carlos Souza** |
| `/cliente/pedidos`, `/assinatura`, `/cartoes`, `/perfil` | 200 autenticado, **Sair** visível, sem Visitante |
| Logout (aside desktop) | Token limpo; volta `/` |
| Guard após logout → `/cliente/perfil` | Redirect login com `next` |
| Login Usuário 2 | Entra em `/cliente/pedidos` |

## Status FO atualizado

| ID | Status | Notas |
|----|--------|-------|
| FO-002 | **PASS** | Mock users + Bearer + redirect pós-login |
| FO-010 | **PASS** | Logout limpa token; guard bloqueia `/cliente/*` |
| FO-006 | **PASS parcial** | Perfil abre autenticado; ainda há `cliente-001` hardcoded no PATCH |
| FO-007 | **PASS** | Página autenticada OK (pode mostrar empty state) |
| FO-008 | **PASS** | Pedidos autenticados OK |
| FO-009 | **PASS** | Cartões autenticados OK |
| FO-003 | **PASS parcial** | Sem mudança (só solicitação) |
| FO-004/005 | **INCONCLUSIVO** | Checkout SSR ainda sem labels Pix/Cartão (métodos vêm do perfil server-side) |
| FO-001 | **NÃO EXECUTADO** | E2E compra completa pendente |

## Observações

1. **API remota Vercel**: CORS bloqueia `Origin: http://127.0.0.1:3000` — usar API local em dev.
2. **Checkout**: SSR usa `getCustomerProfile()` sem Bearer do `localStorage`; opções de pagamento podem ficar vazias no primeiro paint.
3. Próximo: IDs dinâmicos no lugar de `cliente-001`; opcionalmente repassar token em fetches server-side.
